(function() {

  Cookies.set(
    "browser.timezone",
    Intl.DateTimeFormat().resolvedOptions().timeZone,
    {
      expires: 365,
      path: '/',
      sameSite: 'Lax',
      secure: <%= !::Rails.env.development? && !::Rails.env.test? %>
    }
  );
})();
